# bank-prompt-chain
A 5-stage intelligent prompt chain for processing banking customer queries, from intent detection to response generation. The system interprets user intent, maps categories, selects the most relevant category, extracts actionable details, and generates a concise, helpful reply. Built using Together AI.
